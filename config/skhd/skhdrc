.blacklist [
  "terminal"
]

# open a terminal
alt - t : [[ -z $(pgrep wez) ]] && open -a "$HOME/Applications/Home Manager Apps/WezTerm.app" || wezterm cli spawn --new-window
shift + alt - return : emacsclient -c
